apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: app
  ports:
    - protocol: TCP
      port: 80           # port na zewnątrz klastra
      targetPort: 8080    # port kontenera
  type: LoadBalancer
